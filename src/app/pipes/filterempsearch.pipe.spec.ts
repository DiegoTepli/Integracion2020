import { FilterempsearchPipe } from './filterempsearch.pipe';

describe('FilterempsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterempsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
